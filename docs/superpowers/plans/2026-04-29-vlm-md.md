# vlm.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the vlm.md static site — a bilingual (zh/en) VLM recipe cookbook for agent developers, deployable to Cloudflare Pages.

**Architecture:** Astro 4 static site with MDX content collections. Recipes are MDX files in `src/content/recipes/{zh,en}/`. All pages are statically generated at build time. No backend, no runtime server.

**Tech Stack:** Astro 4, `@astrojs/tailwind`, `@astrojs/mdx`, Tailwind CSS v3, Pagefind (static search), Cloudflare Pages

---

## File Map

| File | Responsibility |
|---|---|
| `astro.config.mjs` | Astro config: integrations, output = static |
| `tailwind.config.mjs` | Tailwind config with content paths |
| `src/content/config.ts` | Recipe collection Zod schema |
| `src/layouts/BaseLayout.astro` | HTML shell, `<head>` meta, Google Fonts |
| `src/components/Header.astro` | Nav bar with lang toggle |
| `src/components/Footer.astro` | Footer with GitHub link |
| `src/components/RecipeCard.astro` | Card shown on list page |
| `src/components/Search.astro` | Pagefind search widget |
| `src/pages/index.astro` | Redirect `/` → `/zh` |
| `src/pages/zh/index.astro` | Chinese home page |
| `src/pages/zh/recipes/index.astro` | Chinese recipe list |
| `src/pages/zh/recipes/[...slug].astro` | Chinese recipe detail |
| `src/pages/en/index.astro` | English home page |
| `src/pages/en/recipes/index.astro` | English recipe list |
| `src/pages/en/recipes/[...slug].astro` | English recipe detail |
| `src/content/recipes/zh/invoice-extraction.mdx` | Seed recipe (zh) |
| `src/content/recipes/en/invoice-extraction.mdx` | Seed recipe (en) |
| `public/_redirects` | Cloudflare Pages redirect rules |
| `CONTRIBUTING.md` | English contribution guide |
| `CONTRIBUTING.zh.md` | Chinese contribution guide |

---

## Task 1: Project Scaffolding

**Files:**
- Create: `package.json`, `astro.config.mjs`, `tailwind.config.mjs`, `tsconfig.json`

- [ ] **Step 1: Initialize Astro project**

```bash
cd /Users/chenzhen48/Workspace/vlm.md
npm create astro@latest . -- --template minimal --typescript strict --no-install --no-git
```

Answer prompts: template = minimal, TypeScript = strict, no install, no git init.

- [ ] **Step 2: Install dependencies**

```bash
npm install
npm install @astrojs/tailwind @astrojs/mdx tailwindcss
```

- [ ] **Step 3: Replace `astro.config.mjs`**

```javascript
import { defineConfig } from 'astro/config';
import tailwind from '@astrojs/tailwind';
import mdx from '@astrojs/mdx';

export default defineConfig({
  site: 'https://vlm.md',
  output: 'static',
  integrations: [tailwind(), mdx()],
});
```

- [ ] **Step 4: Replace `tailwind.config.mjs`**

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step 5: Verify TypeScript config**

`tsconfig.json` should already have `"extends": "astro/tsconfigs/strict"`. If not, replace:

```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "strictNullChecks": true
  }
}
```

- [ ] **Step 6: Verify it builds**

```bash
npx astro build
```

Expected: Build completes with no errors. Ignore "no pages" warnings.

- [ ] **Step 7: Commit**

```bash
git init
git add astro.config.mjs tailwind.config.mjs tsconfig.json package.json package-lock.json
git commit -m "feat: initialize Astro project with Tailwind and MDX"
```

---

## Task 2: Content Collection Schema

**Files:**
- Create: `src/content/config.ts`
- Create: `src/content/recipes/zh/.gitkeep`, `src/content/recipes/en/.gitkeep`

- [ ] **Step 1: Create `src/content/config.ts`**

```typescript
import { defineCollection, z } from 'astro:content';

const CATEGORIES = [
  'document-understanding',
  'ui-grounding',
  'chart-table',
  'computer-use',
  'multi-image',
  'structured-output',
] as const;

const recipes = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    description: z.string(),
    category: z.enum(CATEGORIES),
    lang: z.enum(['zh', 'en']),
    models: z.array(z.string()),
    difficulty: z.enum(['beginner', 'intermediate', 'advanced']),
    date: z.date(),
    author: z.string().optional(),
    slug: z.string(),
  }),
});

export const collections = { recipes };

export const CATEGORY_LABELS: Record<typeof CATEGORIES[number], { zh: string; en: string }> = {
  'document-understanding': { zh: '文档理解', en: 'Document Understanding' },
  'ui-grounding': { zh: 'UI 操控', en: 'UI Grounding' },
  'chart-table': { zh: '图表/表格', en: 'Chart & Table' },
  'computer-use': { zh: 'Computer Use', en: 'Computer Use' },
  'multi-image': { zh: '多图推理', en: 'Multi-image Reasoning' },
  'structured-output': { zh: '结构化输出', en: 'Structured Output' },
};
```

- [ ] **Step 2: Create placeholder directories**

```bash
mkdir -p src/content/recipes/zh src/content/recipes/en
touch src/content/recipes/zh/.gitkeep src/content/recipes/en/.gitkeep
```

- [ ] **Step 3: Type-check**

```bash
npx astro check
```

Expected: No errors.

- [ ] **Step 4: Commit**

```bash
git add src/content/
git commit -m "feat: add recipe content collection schema"
```

---

## Task 3: Base Layout and Shared Components

**Files:**
- Create: `src/layouts/BaseLayout.astro`
- Create: `src/components/Header.astro`
- Create: `src/components/Footer.astro`
- Create: `src/styles/global.css`

- [ ] **Step 1: Create `src/styles/global.css`**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap');
```

- [ ] **Step 2: Create `src/layouts/BaseLayout.astro`**

```astro
---
interface Props {
  title: string;
  description?: string;
  lang?: 'zh' | 'en';
}

const { title, description = 'VLM recipes for agent developers', lang = 'zh' } = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title} | vlm.md</title>
  </head>
  <body class="bg-gray-950 text-gray-100 min-h-screen flex flex-col font-sans">
    <slot name="header" />
    <main class="flex-1 max-w-5xl mx-auto w-full px-4 py-8">
      <slot />
    </main>
    <slot name="footer" />
  </body>
</html>
```

- [ ] **Step 3: Create `src/components/Header.astro`**

```astro
---
interface Props {
  lang: 'zh' | 'en';
  currentPath?: string;
}

const { lang, currentPath = '' } = Astro.props;

const otherLang = lang === 'zh' ? 'en' : 'zh';
const otherPath = currentPath.replace(`/${lang}/`, `/${otherLang}/`);

const nav = {
  zh: { home: '首页', recipes: '食谱', about: '关于' },
  en: { home: 'Home', recipes: 'Recipes', about: 'About' },
}[lang];
---

<header class="border-b border-gray-800 sticky top-0 bg-gray-950/95 backdrop-blur z-50">
  <div class="max-w-5xl mx-auto px-4 h-14 flex items-center justify-between">
    <a href={`/${lang}`} class="font-mono font-semibold text-lg text-white hover:text-violet-400 transition-colors">
      vlm<span class="text-violet-400">.md</span>
    </a>
    <nav class="flex items-center gap-6 text-sm">
      <a href={`/${lang}/recipes`} class="text-gray-400 hover:text-white transition-colors">{nav.recipes}</a>
      <a
        href={otherPath || `/${otherLang}`}
        class="px-2 py-1 rounded border border-gray-700 text-xs text-gray-400 hover:border-violet-500 hover:text-violet-400 transition-colors font-mono"
      >
        {otherLang === 'en' ? 'EN' : '中文'}
      </a>
    </nav>
  </div>
</header>
```

- [ ] **Step 4: Create `src/components/Footer.astro`**

```astro
---
interface Props {
  lang: 'zh' | 'en';
}

const { lang } = Astro.props;

const text = {
  zh: { contribute: '贡献食谱', builtWith: '用 Astro 构建，部署于 Cloudflare Pages' },
  en: { contribute: 'Contribute a Recipe', builtWith: 'Built with Astro, deployed on Cloudflare Pages' },
}[lang];
---

<footer class="border-t border-gray-800 mt-16">
  <div class="max-w-5xl mx-auto px-4 py-8 flex flex-col sm:flex-row items-center justify-between gap-4 text-sm text-gray-500">
    <span class="font-mono">vlm<span class="text-violet-400">.md</span></span>
    <a href="https://github.com/vlm-md/vlm.md/blob/main/CONTRIBUTING.md" class="hover:text-violet-400 transition-colors">
      {text.contribute}
    </a>
    <span>{text.builtWith}</span>
  </div>
</footer>
```

- [ ] **Step 5: Create `public/favicon.svg`**

```bash
cat > public/favicon.svg << 'EOF'
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="6" fill="#7c3aed"/>
  <text x="4" y="24" font-family="monospace" font-size="18" font-weight="bold" fill="white">.md</text>
</svg>
EOF
```

- [ ] **Step 6: Type-check and build**

```bash
npx astro check && npx astro build
```

Expected: No errors.

- [ ] **Step 7: Commit**

```bash
git add src/layouts/ src/components/ src/styles/ public/favicon.svg
git commit -m "feat: add base layout, header, and footer components"
```

---

## Task 4: Recipe Card Component

**Files:**
- Create: `src/components/RecipeCard.astro`

- [ ] **Step 1: Create `src/components/RecipeCard.astro`**

```astro
---
import type { CollectionEntry } from 'astro:content';
import { CATEGORY_LABELS } from '../content/config';

interface Props {
  recipe: CollectionEntry<'recipes'>;
  lang: 'zh' | 'en';
}

const { recipe, lang } = Astro.props;
const { title, description, category, models, difficulty } = recipe.data;

const categoryLabel = CATEGORY_LABELS[category][lang];

const difficultyColor = {
  beginner: 'text-green-400 border-green-800',
  intermediate: 'text-yellow-400 border-yellow-800',
  advanced: 'text-red-400 border-red-800',
}[difficulty];

const difficultyLabel = {
  zh: { beginner: '入门', intermediate: '进阶', advanced: '高级' },
  en: { beginner: 'Beginner', intermediate: 'Intermediate', advanced: 'Advanced' },
}[lang][difficulty];
---

<a
  href={`/${lang}/recipes/${recipe.data.slug}`}
  class="block rounded-lg border border-gray-800 bg-gray-900 p-5 hover:border-violet-700 transition-all group"
>
  <div class="flex items-start justify-between gap-3 mb-3">
    <span class="text-xs px-2 py-0.5 rounded border border-gray-700 text-gray-400 font-mono">
      {categoryLabel}
    </span>
    <span class={`text-xs px-2 py-0.5 rounded border font-mono ${difficultyColor}`}>
      {difficultyLabel}
    </span>
  </div>
  <h3 class="font-semibold text-white group-hover:text-violet-300 transition-colors mb-2 leading-snug">
    {title}
  </h3>
  <p class="text-sm text-gray-400 line-clamp-2 mb-4">{description}</p>
  <div class="flex flex-wrap gap-1">
    {models.map((model) => (
      <span class="text-xs px-1.5 py-0.5 rounded bg-gray-800 text-gray-400 font-mono">
        {model}
      </span>
    ))}
  </div>
</a>
```

- [ ] **Step 2: Type-check**

```bash
npx astro check
```

Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/components/RecipeCard.astro
git commit -m "feat: add RecipeCard component"
```

---

## Task 5: Home Pages (zh + en)

**Files:**
- Create: `src/pages/index.astro`
- Create: `src/pages/zh/index.astro`
- Create: `src/pages/en/index.astro`

- [ ] **Step 1: Create `src/pages/index.astro`** (redirect to /zh)

```astro
---
return Astro.redirect('/zh', 301);
---
```

- [ ] **Step 2: Create `src/pages/zh/index.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import RecipeCard from '../../components/RecipeCard.astro';
import { getCollection } from 'astro:content';

const lang = 'zh' as const;
const recipes = await getCollection('recipes', (r) => r.data.lang === 'zh');
const featured = recipes.slice(0, 3);

const categories = [
  { key: 'document-understanding', label: '文档理解', icon: '📄' },
  { key: 'ui-grounding', label: 'UI 操控', icon: '🖱️' },
  { key: 'chart-table', label: '图表/表格', icon: '📊' },
  { key: 'computer-use', label: 'Computer Use', icon: '💻' },
  { key: 'multi-image', label: '多图推理', icon: '🖼️' },
  { key: 'structured-output', label: '结构化输出', icon: '🗂️' },
];
---

<BaseLayout title="VLM 实战食谱库" lang={lang}>
  <Header slot="header" lang={lang} currentPath="/zh/" />
  <Footer slot="footer" lang={lang} />

  <section class="py-16 text-center">
    <p class="text-violet-400 font-mono text-sm mb-4">vlm.md</p>
    <h1 class="text-4xl sm:text-5xl font-bold text-white mb-6 leading-tight">
      VLM × Agent<br />
      <span class="text-violet-400">实战食谱库</span>
    </h1>
    <p class="text-gray-400 text-lg max-w-2xl mx-auto mb-8">
      每一篇食谱告诉你：用哪个模型、怎么写 prompt、代码怎么写、踩过哪些坑。
      专为在 agent 中集成 VLM 的开发者设计。
    </p>
    <div class="flex gap-4 justify-center">
      <a href="/zh/recipes" class="px-6 py-2.5 bg-violet-600 hover:bg-violet-500 text-white rounded-lg font-medium transition-colors">
        浏览食谱
      </a>
      <a href="https://github.com/vlm-md/vlm.md/blob/main/CONTRIBUTING.zh.md" class="px-6 py-2.5 border border-gray-700 hover:border-violet-500 text-gray-300 rounded-lg font-medium transition-colors">
        贡献食谱
      </a>
    </div>
  </section>

  <section class="mb-16">
    <h2 class="text-lg font-semibold text-gray-300 mb-6">分类浏览</h2>
    <div class="grid grid-cols-2 sm:grid-cols-3 gap-3">
      {categories.map((cat) => (
        <a
          href={`/zh/recipes?category=${cat.key}`}
          class="flex items-center gap-3 p-4 rounded-lg border border-gray-800 bg-gray-900 hover:border-violet-700 transition-colors"
        >
          <span class="text-2xl">{cat.icon}</span>
          <span class="text-sm font-medium text-gray-300">{cat.label}</span>
        </a>
      ))}
    </div>
  </section>

  {featured.length > 0 && (
    <section>
      <h2 class="text-lg font-semibold text-gray-300 mb-6">最新食谱</h2>
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {featured.map((recipe) => (
          <RecipeCard recipe={recipe} lang={lang} />
        ))}
      </div>
    </section>
  )}
</BaseLayout>
```

- [ ] **Step 3: Create `src/pages/en/index.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import RecipeCard from '../../components/RecipeCard.astro';
import { getCollection } from 'astro:content';

const lang = 'en' as const;
const recipes = await getCollection('recipes', (r) => r.data.lang === 'en');
const featured = recipes.slice(0, 3);

const categories = [
  { key: 'document-understanding', label: 'Document Understanding', icon: '📄' },
  { key: 'ui-grounding', label: 'UI Grounding', icon: '🖱️' },
  { key: 'chart-table', label: 'Chart & Table', icon: '📊' },
  { key: 'computer-use', label: 'Computer Use', icon: '💻' },
  { key: 'multi-image', label: 'Multi-image Reasoning', icon: '🖼️' },
  { key: 'structured-output', label: 'Structured Output', icon: '🗂️' },
];
---

<BaseLayout title="VLM Recipe Cookbook" lang={lang}>
  <Header slot="header" lang={lang} currentPath="/en/" />
  <Footer slot="footer" lang={lang} />

  <section class="py-16 text-center">
    <p class="text-violet-400 font-mono text-sm mb-4">vlm.md</p>
    <h1 class="text-4xl sm:text-5xl font-bold text-white mb-6 leading-tight">
      VLM × Agent<br />
      <span class="text-violet-400">Recipe Cookbook</span>
    </h1>
    <p class="text-gray-400 text-lg max-w-2xl mx-auto mb-8">
      Battle-tested recipes for using VLMs in agent systems. Which model, which prompt,
      working code, and the gotchas we hit in production.
    </p>
    <div class="flex gap-4 justify-center">
      <a href="/en/recipes" class="px-6 py-2.5 bg-violet-600 hover:bg-violet-500 text-white rounded-lg font-medium transition-colors">
        Browse Recipes
      </a>
      <a href="https://github.com/vlm-md/vlm.md/blob/main/CONTRIBUTING.md" class="px-6 py-2.5 border border-gray-700 hover:border-violet-500 text-gray-300 rounded-lg font-medium transition-colors">
        Contribute
      </a>
    </div>
  </section>

  <section class="mb-16">
    <h2 class="text-lg font-semibold text-gray-300 mb-6">Browse by Category</h2>
    <div class="grid grid-cols-2 sm:grid-cols-3 gap-3">
      {categories.map((cat) => (
        <a
          href={`/en/recipes?category=${cat.key}`}
          class="flex items-center gap-3 p-4 rounded-lg border border-gray-800 bg-gray-900 hover:border-violet-700 transition-colors"
        >
          <span class="text-2xl">{cat.icon}</span>
          <span class="text-sm font-medium text-gray-300">{cat.label}</span>
        </a>
      ))}
    </div>
  </section>

  {featured.length > 0 && (
    <section>
      <h2 class="text-lg font-semibold text-gray-300 mb-6">Latest Recipes</h2>
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {featured.map((recipe) => (
          <RecipeCard recipe={recipe} lang={lang} />
        ))}
      </div>
    </section>
  )}
</BaseLayout>
```

- [ ] **Step 4: Type-check and build**

```bash
npx astro check && npx astro build
```

Expected: Build succeeds. `dist/` contains `index.html`, `zh/index.html`, `en/index.html`.

- [ ] **Step 5: Verify redirect works**

```bash
npx astro preview
```

Open `http://localhost:4321/` — should redirect to `/zh`. Ctrl+C when done.

- [ ] **Step 6: Commit**

```bash
git add src/pages/
git commit -m "feat: add home pages (zh + en) and root redirect"
```

---

## Task 6: Recipe List Pages (zh + en)

**Files:**
- Create: `src/pages/zh/recipes/index.astro`
- Create: `src/pages/en/recipes/index.astro`

- [ ] **Step 1: Create `src/pages/zh/recipes/index.astro`**

```astro
---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import RecipeCard from '../../../components/RecipeCard.astro';
import { getCollection } from 'astro:content';
import { CATEGORY_LABELS } from '../../../content/config';

const lang = 'zh' as const;
const allRecipes = await getCollection('recipes', (r) => r.data.lang === 'zh');
const sorted = allRecipes.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const categories = Object.entries(CATEGORY_LABELS).map(([key, labels]) => ({
  key,
  label: labels.zh,
}));
---

<BaseLayout title="所有食谱" lang={lang}>
  <Header slot="header" lang={lang} currentPath="/zh/recipes" />
  <Footer slot="footer" lang={lang} />

  <div class="mb-8">
    <h1 class="text-3xl font-bold text-white mb-2">所有食谱</h1>
    <p class="text-gray-400">{sorted.length} 个食谱</p>
  </div>

  <div class="flex gap-2 flex-wrap mb-8" id="category-filters">
    <button
      data-category="all"
      class="category-btn active px-3 py-1.5 rounded-lg text-sm border border-violet-500 text-violet-400 font-mono"
    >
      全部
    </button>
    {categories.map((cat) => (
      <button
        data-category={cat.key}
        class="category-btn px-3 py-1.5 rounded-lg text-sm border border-gray-700 text-gray-400 font-mono hover:border-violet-500 hover:text-violet-400 transition-colors"
      >
        {cat.label}
      </button>
    ))}
  </div>

  <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4" id="recipe-grid">
    {sorted.map((recipe) => (
      <div data-category={recipe.data.category} class="recipe-item">
        <RecipeCard recipe={recipe} lang={lang} />
      </div>
    ))}
  </div>
</BaseLayout>

<script>
  const buttons = document.querySelectorAll<HTMLButtonElement>('.category-btn');
  const items = document.querySelectorAll<HTMLElement>('.recipe-item');

  buttons.forEach((btn) => {
    btn.addEventListener('click', () => {
      const selected = btn.dataset.category!;
      buttons.forEach((b) => b.classList.remove('active', 'border-violet-500', 'text-violet-400'));
      btn.classList.add('active', 'border-violet-500', 'text-violet-400');
      items.forEach((item) => {
        const show = selected === 'all' || item.dataset.category === selected;
        item.style.display = show ? '' : 'none';
      });
    });
  });
</script>
```

- [ ] **Step 2: Create `src/pages/en/recipes/index.astro`**

```astro
---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import RecipeCard from '../../../components/RecipeCard.astro';
import { getCollection } from 'astro:content';
import { CATEGORY_LABELS } from '../../../content/config';

const lang = 'en' as const;
const allRecipes = await getCollection('recipes', (r) => r.data.lang === 'en');
const sorted = allRecipes.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const categories = Object.entries(CATEGORY_LABELS).map(([key, labels]) => ({
  key,
  label: labels.en,
}));
---

<BaseLayout title="All Recipes" lang={lang}>
  <Header slot="header" lang={lang} currentPath="/en/recipes" />
  <Footer slot="footer" lang={lang} />

  <div class="mb-8">
    <h1 class="text-3xl font-bold text-white mb-2">All Recipes</h1>
    <p class="text-gray-400">{sorted.length} recipes</p>
  </div>

  <div class="flex gap-2 flex-wrap mb-8" id="category-filters">
    <button
      data-category="all"
      class="category-btn active px-3 py-1.5 rounded-lg text-sm border border-violet-500 text-violet-400 font-mono"
    >
      All
    </button>
    {categories.map((cat) => (
      <button
        data-category={cat.key}
        class="category-btn px-3 py-1.5 rounded-lg text-sm border border-gray-700 text-gray-400 font-mono hover:border-violet-500 hover:text-violet-400 transition-colors"
      >
        {cat.label}
      </button>
    ))}
  </div>

  <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4" id="recipe-grid">
    {sorted.map((recipe) => (
      <div data-category={recipe.data.category} class="recipe-item">
        <RecipeCard recipe={recipe} lang={lang} />
      </div>
    ))}
  </div>
</BaseLayout>

<script>
  const buttons = document.querySelectorAll<HTMLButtonElement>('.category-btn');
  const items = document.querySelectorAll<HTMLElement>('.recipe-item');

  buttons.forEach((btn) => {
    btn.addEventListener('click', () => {
      const selected = btn.dataset.category!;
      buttons.forEach((b) => b.classList.remove('active', 'border-violet-500', 'text-violet-400'));
      btn.classList.add('active', 'border-violet-500', 'text-violet-400');
      items.forEach((item) => {
        const show = selected === 'all' || item.dataset.category === selected;
        item.style.display = show ? '' : 'none';
      });
    });
  });
</script>
```

- [ ] **Step 3: Build and verify**

```bash
npx astro build
```

Expected: `dist/zh/recipes/index.html` and `dist/en/recipes/index.html` both generated.

- [ ] **Step 4: Commit**

```bash
git add src/pages/zh/recipes/index.astro src/pages/en/recipes/index.astro
git commit -m "feat: add recipe list pages with category filter"
```

---

## Task 7: Recipe Detail Pages (zh + en)

**Files:**
- Create: `src/pages/zh/recipes/[...slug].astro`
- Create: `src/pages/en/recipes/[...slug].astro`

- [ ] **Step 1: Create `src/pages/zh/recipes/[...slug].astro`**

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import { CATEGORY_LABELS } from '../../../content/config';

export async function getStaticPaths() {
  const recipes = await getCollection('recipes', (r) => r.data.lang === 'zh');
  return recipes.map((recipe) => ({
    params: { slug: recipe.data.slug },
    props: { recipe },
  }));
}

const { recipe } = Astro.props;
const { Content } = await recipe.render();
const { title, description, category, models, difficulty, date, author } = recipe.data;
const lang = 'zh' as const;

const categoryLabel = CATEGORY_LABELS[category].zh;
const difficultyLabel = { beginner: '入门', intermediate: '进阶', advanced: '高级' }[difficulty];
const difficultyColor = { beginner: 'text-green-400', intermediate: 'text-yellow-400', advanced: 'text-red-400' }[difficulty];
---

<BaseLayout title={title} description={description} lang={lang}>
  <Header slot="header" lang={lang} currentPath={`/zh/recipes/${recipe.data.slug}`} />
  <Footer slot="footer" lang={lang} />

  <article>
    <div class="mb-8">
      <div class="flex items-center gap-3 mb-4">
        <a href="/zh/recipes" class="text-sm text-gray-500 hover:text-violet-400 transition-colors">← 所有食谱</a>
        <span class="text-gray-700">·</span>
        <span class="text-sm text-gray-500 font-mono">{categoryLabel}</span>
        <span class="text-gray-700">·</span>
        <span class={`text-sm font-mono ${difficultyColor}`}>{difficultyLabel}</span>
      </div>
      <h1 class="text-3xl font-bold text-white mb-3">{title}</h1>
      <p class="text-gray-400 text-lg mb-4">{description}</p>
      <div class="flex items-center gap-4 text-sm text-gray-500">
        <span>{date.toLocaleDateString('zh-CN')}</span>
        {author && <span>· {author}</span>}
        <span>· 推荐模型：</span>
        {models.map((m) => (
          <span class="px-2 py-0.5 rounded bg-gray-800 text-gray-300 font-mono text-xs">{m}</span>
        ))}
      </div>
    </div>

    <div class="prose prose-invert prose-violet max-w-none
      prose-headings:font-semibold prose-headings:text-white
      prose-code:font-mono prose-code:bg-gray-800 prose-code:text-violet-300 prose-code:px-1 prose-code:rounded
      prose-pre:bg-gray-900 prose-pre:border prose-pre:border-gray-800
      prose-a:text-violet-400 prose-a:no-underline hover:prose-a:underline
      prose-blockquote:border-violet-500 prose-blockquote:text-gray-400">
      <Content />
    </div>
  </article>
</BaseLayout>
```

- [ ] **Step 2: Create `src/pages/en/recipes/[...slug].astro`**

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import { CATEGORY_LABELS } from '../../../content/config';

export async function getStaticPaths() {
  const recipes = await getCollection('recipes', (r) => r.data.lang === 'en');
  return recipes.map((recipe) => ({
    params: { slug: recipe.data.slug },
    props: { recipe },
  }));
}

const { recipe } = Astro.props;
const { Content } = await recipe.render();
const { title, description, category, models, difficulty, date, author } = recipe.data;
const lang = 'en' as const;

const categoryLabel = CATEGORY_LABELS[category].en;
const difficultyLabel = { beginner: 'Beginner', intermediate: 'Intermediate', advanced: 'Advanced' }[difficulty];
const difficultyColor = { beginner: 'text-green-400', intermediate: 'text-yellow-400', advanced: 'text-red-400' }[difficulty];
---

<BaseLayout title={title} description={description} lang={lang}>
  <Header slot="header" lang={lang} currentPath={`/en/recipes/${recipe.data.slug}`} />
  <Footer slot="footer" lang={lang} />

  <article>
    <div class="mb-8">
      <div class="flex items-center gap-3 mb-4">
        <a href="/en/recipes" class="text-sm text-gray-500 hover:text-violet-400 transition-colors">← All Recipes</a>
        <span class="text-gray-700">·</span>
        <span class="text-sm text-gray-500 font-mono">{categoryLabel}</span>
        <span class="text-gray-700">·</span>
        <span class={`text-sm font-mono ${difficultyColor}`}>{difficultyLabel}</span>
      </div>
      <h1 class="text-3xl font-bold text-white mb-3">{title}</h1>
      <p class="text-gray-400 text-lg mb-4">{description}</p>
      <div class="flex items-center gap-4 text-sm text-gray-500">
        <span>{date.toLocaleDateString('en-US')}</span>
        {author && <span>· {author}</span>}
        <span>· Recommended models:</span>
        {models.map((m) => (
          <span class="px-2 py-0.5 rounded bg-gray-800 text-gray-300 font-mono text-xs">{m}</span>
        ))}
      </div>
    </div>

    <div class="prose prose-invert prose-violet max-w-none
      prose-headings:font-semibold prose-headings:text-white
      prose-code:font-mono prose-code:bg-gray-800 prose-code:text-violet-300 prose-code:px-1 prose-code:rounded
      prose-pre:bg-gray-900 prose-pre:border prose-pre:border-gray-800
      prose-a:text-violet-400 prose-a:no-underline hover:prose-a:underline
      prose-blockquote:border-violet-500 prose-blockquote:text-gray-400">
      <Content />
    </div>
  </article>
</BaseLayout>
```

- [ ] **Step 3: Install Tailwind Typography for prose styles**

```bash
npm install @tailwindcss/typography
```

Update `tailwind.config.mjs`:

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
    },
  },
  plugins: [require('@tailwindcss/typography')],
};
```

- [ ] **Step 4: Build**

```bash
npx astro build
```

Expected: Build succeeds (recipe detail pages are generated only when seed recipes exist — add them in Task 9 first, then rebuild to verify).

- [ ] **Step 5: Commit**

```bash
git add src/pages/zh/recipes/ src/pages/en/recipes/ tailwind.config.mjs package.json package-lock.json
git commit -m "feat: add recipe detail pages with prose styling"
```

---

## Task 8: Cloudflare Pages Configuration

**Files:**
- Create: `public/_redirects`
- Create: `wrangler.toml`

- [ ] **Step 1: Create `public/_redirects`**

```
/  /zh  301
```

This handles the root redirect at the CDN level (faster than Astro meta-redirect).

- [ ] **Step 2: Create `wrangler.toml`**

```toml
name = "vlm-md"
compatibility_date = "2024-01-01"

[site]
bucket = "./dist"
```

- [ ] **Step 3: Verify build output is compatible**

```bash
npx astro build && ls dist/
```

Expected: `dist/` contains `index.html`, `zh/`, `en/`, `_redirects`.

- [ ] **Step 4: Commit**

```bash
git add public/_redirects wrangler.toml
git commit -m "feat: add Cloudflare Pages configuration"
```

**Step 5: Manual Cloudflare Pages setup (one-time, in browser)**

In the Cloudflare dashboard:
1. Go to **Pages** → **Create a project** → **Connect to Git**
2. Select this repository
3. Build settings:
   - Build command: `npm run build`
   - Build output directory: `dist`
4. Add environment variable: none needed
5. Click **Save and Deploy**
6. In **Custom domains**, add `vlm.md` and `www.vlm.md`

---

## Task 9: Seed Recipes (zh + en)

**Files:**
- Create: `src/content/recipes/zh/invoice-extraction.mdx`
- Create: `src/content/recipes/en/invoice-extraction.mdx`

- [ ] **Step 1: Create `src/content/recipes/zh/invoice-extraction.mdx`**

```mdx
---
title: 从发票图片中提取结构化数据
description: 让 agent 自动从扫描发票中提取供应商、金额、日期等字段，输出 JSON。包含真实踩坑记录。
category: document-understanding
lang: zh
slug: invoice-extraction
models:
  - GPT-4o
  - Claude 3.5 Sonnet
  - Gemini 1.5 Pro
difficulty: beginner
date: 2026-04-29
author: vlm.md
---

## 场景

你的 agent 需要处理用户上传的发票图片（PDF 扫描件或手机拍照），并自动提取以下字段：

- 供应商名称
- 发票号码
- 开票日期
- 税前金额、税额、税后总额
- 付款期限（如有）

最终输出结构化 JSON，供下游系统（ERP、记账软件）使用。

## 推荐模型

| 模型 | 适用场景 |
|---|---|
| **GPT-4o** | 最均衡，对中文发票支持好 |
| **Claude 3.5 Sonnet** | 结构化输出更稳定，JSON 格式错误率低 |
| **Gemini 1.5 Pro** | 处理长文档或多页发票性价比高 |

中文发票优先用 GPT-4o 或 Claude，英文发票三者差距不大。

## Prompt 模板

```text
你是一个发票信息提取专家。请从图片中提取以下信息，严格按照 JSON 格式返回，不要有任何多余文字。

必须提取的字段：
- vendor_name: 供应商/卖方名称
- invoice_number: 发票号码
- invoice_date: 开票日期 (YYYY-MM-DD 格式)
- subtotal: 税前金额 (数字，不含货币符号)
- tax_amount: 税额 (数字，不含货币符号)
- total_amount: 税后总额 (数字，不含货币符号)
- currency: 货币代码 (如 CNY, USD)
- due_date: 付款期限 (YYYY-MM-DD，如无则为 null)

如果某字段在图片中不存在，返回 null。
```

## 代码示例

```python
import base64
import json
from pathlib import Path
from openai import OpenAI

client = OpenAI()

def extract_invoice(image_path: str) -> dict:
    image_data = base64.b64encode(Path(image_path).read_bytes()).decode()
    
    # 根据文件扩展名判断 MIME type
    suffix = Path(image_path).suffix.lower()
    mime_type = {"jpg": "image/jpeg", "jpeg": "image/jpeg", "png": "image/png", "pdf": "application/pdf"}.get(suffix[1:], "image/jpeg")
    
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[
            {
                "role": "user",
                "content": [
                    {
                        "type": "image_url",
                        "image_url": {"url": f"data:{mime_type};base64,{image_data}"},
                    },
                    {
                        "type": "text",
                        "text": """你是一个发票信息提取专家。请从图片中提取以下信息，严格按照 JSON 格式返回，不要有任何多余文字。

必须提取的字段：
- vendor_name: 供应商/卖方名称
- invoice_number: 发票号码
- invoice_date: 开票日期 (YYYY-MM-DD 格式)
- subtotal: 税前金额 (数字，不含货币符号)
- tax_amount: 税额 (数字，不含货币符号)
- total_amount: 税后总额 (数字，不含货币符号)
- currency: 货币代码 (如 CNY, USD)
- due_date: 付款期限 (YYYY-MM-DD，如无则为 null)

如果某字段在图片中不存在，返回 null。""",
                    },
                ],
            }
        ],
        max_tokens=512,
    )
    
    raw = response.choices[0].message.content.strip()
    # 去掉模型有时会加的 markdown 代码块
    if raw.startswith("```"):
        raw = raw.split("```")[1]
        if raw.startswith("json"):
            raw = raw[4:]
    
    return json.loads(raw.strip())


if __name__ == "__main__":
    result = extract_invoice("invoice.jpg")
    print(json.dumps(result, ensure_ascii=False, indent=2))
```

运行：

```bash
pip install openai
python extract_invoice.py
```

预期输出：

```json
{
  "vendor_name": "上海某某科技有限公司",
  "invoice_number": "31234567",
  "invoice_date": "2024-03-15",
  "subtotal": 10000.00,
  "tax_amount": 900.00,
  "total_amount": 10900.00,
  "currency": "CNY",
  "due_date": null
}
```

## 踩坑记录

**坑 1：模型返回 markdown 代码块而不是纯 JSON**

GPT-4o 偶尔会在 JSON 外面套一层 `` ```json ... ``` ``，导致 `json.loads()` 失败。上面的代码已经处理了这种情况，但更稳妥的做法是用 `response_format={"type": "json_object"}` 参数（需要在 system prompt 中提到 JSON）：

```python
response = client.chat.completions.create(
    model="gpt-4o",
    response_format={"type": "json_object"},
    messages=[
        {"role": "system", "content": "你是发票提取助手，只输出 JSON。"},
        {"role": "user", "content": [...]},
    ],
)
```

**坑 2：手机拍照的发票倾斜或光线差**

VLM 对轻微倾斜（<15°）容忍度较好，但强烈建议在传给 VLM 前做一次预处理：

```python
from PIL import Image
import pytesseract  # 仅用于检测，不用于提取

def preprocess_image(path: str) -> str:
    """简单压缩，防止超出 API 大小限制"""
    img = Image.open(path)
    if max(img.size) > 2048:
        img.thumbnail((2048, 2048))
        path = path.replace(".", "_resized.")
        img.save(path)
    return path
```

**坑 3：多页 PDF**

OpenAI API 目前不直接支持 PDF 文件。需要先把 PDF 转成图片：

```python
from pdf2image import convert_from_path

def pdf_to_images(pdf_path: str) -> list[str]:
    images = convert_from_path(pdf_path, dpi=200)
    paths = []
    for i, img in enumerate(images):
        p = f"/tmp/page_{i}.jpg"
        img.save(p, "JPEG")
        paths.append(p)
    return paths
```

多页发票建议每页单独提取，再合并结果。
```

- [ ] **Step 2: Create `src/content/recipes/en/invoice-extraction.mdx`**

```mdx
---
title: Extract Structured Data from Invoice Images
description: Have your agent automatically extract vendor, amount, date, and other fields from scanned invoices and output JSON. Includes real production gotchas.
category: document-understanding
lang: en
slug: invoice-extraction
models:
  - GPT-4o
  - Claude 3.5 Sonnet
  - Gemini 1.5 Pro
difficulty: beginner
date: 2026-04-29
author: vlm.md
---

## Scenario

Your agent needs to process user-uploaded invoice images (PDF scans or phone photos) and automatically extract:

- Vendor name
- Invoice number
- Invoice date
- Subtotal, tax amount, total
- Payment due date (if present)

Output structured JSON for downstream systems (ERP, accounting software).

## Recommended Models

| Model | When to use |
|---|---|
| **GPT-4o** | Best overall; strong on diverse invoice formats |
| **Claude 3.5 Sonnet** | Most consistent structured output; lowest JSON error rate |
| **Gemini 1.5 Pro** | Best value for multi-page or long documents |

For non-English invoices, prefer GPT-4o or Claude. For English invoices, all three perform similarly.

## Prompt Template

```text
You are an invoice data extraction expert. Extract the following fields from the image and return ONLY valid JSON — no explanation, no markdown.

Required fields:
- vendor_name: seller/vendor name
- invoice_number: invoice ID or number
- invoice_date: date issued (YYYY-MM-DD)
- subtotal: pre-tax amount (number, no currency symbol)
- tax_amount: tax amount (number, no currency symbol)
- total_amount: total including tax (number, no currency symbol)
- currency: currency code (e.g. USD, EUR, CNY)
- due_date: payment due date (YYYY-MM-DD, or null if not present)

Return null for any field not found in the image.
```

## Code

```python
import base64
import json
from pathlib import Path
from openai import OpenAI

client = OpenAI()

PROMPT = """You are an invoice data extraction expert. Extract the following fields from the image and return ONLY valid JSON — no explanation, no markdown.

Required fields:
- vendor_name: seller/vendor name
- invoice_number: invoice ID or number
- invoice_date: date issued (YYYY-MM-DD)
- subtotal: pre-tax amount (number, no currency symbol)
- tax_amount: tax amount (number, no currency symbol)
- total_amount: total including tax (number, no currency symbol)
- currency: currency code (e.g. USD, EUR, CNY)
- due_date: payment due date (YYYY-MM-DD, or null if not present)

Return null for any field not found in the image."""

def extract_invoice(image_path: str) -> dict:
    image_data = base64.b64encode(Path(image_path).read_bytes()).decode()
    suffix = Path(image_path).suffix.lower().lstrip(".")
    mime_type = {"jpg": "image/jpeg", "jpeg": "image/jpeg", "png": "image/png"}.get(suffix, "image/jpeg")

    response = client.chat.completions.create(
        model="gpt-4o",
        response_format={"type": "json_object"},
        messages=[
            {"role": "system", "content": "You are an invoice extraction assistant. Output JSON only."},
            {
                "role": "user",
                "content": [
                    {"type": "image_url", "image_url": {"url": f"data:{mime_type};base64,{image_data}"}},
                    {"type": "text", "text": PROMPT},
                ],
            },
        ],
        max_tokens=512,
    )

    return json.loads(response.choices[0].message.content)


if __name__ == "__main__":
    result = extract_invoice("invoice.jpg")
    print(json.dumps(result, indent=2))
```

Run:

```bash
pip install openai
python extract_invoice.py
```

Expected output:

```json
{
  "vendor_name": "Acme Corp",
  "invoice_number": "INV-2024-0042",
  "invoice_date": "2024-03-15",
  "subtotal": 1000.00,
  "tax_amount": 100.00,
  "total_amount": 1100.00,
  "currency": "USD",
  "due_date": "2024-04-15"
}
```

## Gotchas

**Gotcha 1: Model wraps JSON in a markdown code block**

Without `response_format={"type": "json_object"}`, GPT-4o sometimes wraps the output in `` ```json ... ``` ``. The code above uses `json_object` mode to prevent this. Note: this mode requires mentioning "JSON" somewhere in the system prompt.

**Gotcha 2: Low-quality or skewed phone photos**

VLMs handle light skew (<15°) well, but image size matters. Pre-resize large images before sending:

```python
from PIL import Image

def preprocess(path: str) -> str:
    img = Image.open(path)
    if max(img.size) > 2048:
        img.thumbnail((2048, 2048))
        out = path.replace(".", "_resized.")
        img.save(out)
        return out
    return path
```

**Gotcha 3: Multi-page PDFs**

The OpenAI API doesn't accept PDF files directly. Convert to images first:

```python
from pdf2image import convert_from_path

def pdf_to_images(pdf_path: str) -> list[str]:
    pages = convert_from_path(pdf_path, dpi=200)
    paths = []
    for i, page in enumerate(pages):
        p = f"/tmp/invoice_page_{i}.jpg"
        page.save(p, "JPEG")
        paths.append(p)
    return paths
```

Extract each page separately, then merge results. For multi-page invoices, the total is usually on the last page.
```

- [ ] **Step 3: Build with seed recipes**

```bash
npx astro build
```

Expected: Build succeeds. Recipe detail pages appear at `dist/zh/recipes/invoice-extraction/index.html` and `dist/en/recipes/invoice-extraction/index.html`.

- [ ] **Step 4: Preview locally**

```bash
npx astro preview
```

Visit:
- `http://localhost:4321/zh` — Chinese home with featured recipe
- `http://localhost:4321/zh/recipes/invoice-extraction` — Full recipe detail

- [ ] **Step 5: Commit**

```bash
git add src/content/recipes/
git commit -m "feat: add seed invoice extraction recipe (zh + en)"
```

---

## Task 10: CONTRIBUTING Guides

**Files:**
- Create: `CONTRIBUTING.md`
- Create: `CONTRIBUTING.zh.md`

- [ ] **Step 1: Create `CONTRIBUTING.md`**

```markdown
# Contributing to vlm.md

vlm.md is a community cookbook of battle-tested VLM recipes for agent developers.

## What makes a good recipe?

- **Real scenario**: A task you've actually needed to solve
- **Working code**: Runnable Python (tested, not theoretical)
- **Gotchas**: At least one real production problem you encountered
- **Model recommendations**: Which VLMs you tested and why

## How to submit

1. Fork this repository
2. Create a new MDX file in `src/content/recipes/en/your-recipe-slug.mdx`
3. Follow the frontmatter schema below
4. Open a Pull Request

## Frontmatter schema

```yaml
---
title: Your Recipe Title
description: One sentence describing the task and what makes this recipe useful
category: document-understanding  # see categories below
lang: en
slug: your-recipe-slug            # matches the filename, used in URLs
models:
  - GPT-4o
  - Claude 3.5 Sonnet
difficulty: beginner              # beginner / intermediate / advanced
date: 2024-03-15                  # YYYY-MM-DD
author: your-name                 # optional, shown on recipe page
---
```

## Categories

- `document-understanding` — Invoice, PDF, form extraction
- `ui-grounding` — Web UI element detection, clicking, navigation
- `chart-table` — Chart/graph reading, table extraction
- `computer-use` — Desktop automation, screenshot-based agents
- `multi-image` — Comparing multiple images, sequential screenshots
- `structured-output` — Reliable JSON/structured extraction patterns

## Review criteria

All PRs are reviewed for:
- [ ] Code actually runs (paste a real output in the PR description)
- [ ] Scenario is specific and real (not "extract text from image")
- [ ] At least one gotcha documented
- [ ] Frontmatter validates against the schema

## Chinese recipes

If you write in Chinese, use `lang: zh` and place the file in `src/content/recipes/zh/`. English translation is welcome but not required.
```

- [ ] **Step 2: Create `CONTRIBUTING.zh.md`**

```markdown
# 向 vlm.md 贡献食谱

vlm.md 是一个面向 agent 开发者的 VLM 实战食谱社区。

## 什么是好食谱？

- **真实场景**：你实际遇到并解决过的工程问题
- **可运行代码**：经过测试的 Python 代码，不是伪代码
- **踩坑记录**：至少一个你在生产中遇到的真实问题
- **模型推荐**：你测试过哪些 VLM，为什么选它

## 如何提交

1. Fork 本仓库
2. 在 `src/content/recipes/zh/` 中新建 `你的-slug.mdx` 文件
3. 按照下面的 frontmatter 格式填写
4. 提交 Pull Request

## Frontmatter 格式

```yaml
---
title: 食谱标题
description: 一句话描述任务是什么，这个食谱解决了什么问题
category: document-understanding  # 见下方分类
lang: zh
slug: your-recipe-slug            # 与文件名一致，用于 URL
models:
  - GPT-4o
  - Claude 3.5 Sonnet
difficulty: beginner              # beginner / intermediate / advanced
date: 2024-03-15                  # YYYY-MM-DD
author: 你的名字                   # 可选，会显示在食谱页面
---
```

## 分类说明

- `document-understanding` — 发票、PDF、表单信息提取
- `ui-grounding` — 网页 UI 元素检测、点击、导航
- `chart-table` — 图表读取、表格提取
- `computer-use` — 桌面自动化、基于截图的 agent
- `multi-image` — 多图对比、连续截图推理
- `structured-output` — 稳定的 JSON/结构化输出模式

## 审核标准

所有 PR 将检查：

- [ ] 代码可以运行（请在 PR 描述中粘贴一个真实输出）
- [ ] 场景具体且真实（不接受"从图片中提取文字"这种过于宽泛的描述）
- [ ] 至少记录了一个踩坑
- [ ] frontmatter 格式正确

## 英文食谱

如果你愿意同时提供英文版，请在 `src/content/recipes/en/` 中使用相同 slug 创建英文版本。不做强制要求。
```

- [ ] **Step 3: Commit**

```bash
git add CONTRIBUTING.md CONTRIBUTING.zh.md
git commit -m "docs: add contribution guides (zh + en)"
```

---

## Task 11: Pagefind Static Search

**Files:**
- Modify: `astro.config.mjs`
- Create: `src/components/Search.astro`
- Modify: `src/pages/zh/recipes/index.astro` (add search widget)
- Modify: `src/pages/en/recipes/index.astro` (add search widget)

Pagefind runs after `astro build` and indexes the generated HTML. It requires no server — the search index is static files served alongside the site.

- [ ] **Step 1: Install astro-pagefind**

```bash
npm install astro-pagefind
```

- [ ] **Step 2: Update `astro.config.mjs`**

```javascript
import { defineConfig } from 'astro/config';
import tailwind from '@astrojs/tailwind';
import mdx from '@astrojs/mdx';
import pagefind from 'astro-pagefind';

export default defineConfig({
  site: 'https://vlm.md',
  output: 'static',
  integrations: [tailwind(), mdx(), pagefind()],
});
```

- [ ] **Step 3: Create `src/components/Search.astro`**

```astro
---
interface Props {
  lang: 'zh' | 'en';
}
const { lang } = Astro.props;
const placeholder = lang === 'zh' ? '搜索食谱...' : 'Search recipes...';
---

<div id="search" class="w-full max-w-md">
  <input
    id="pagefind-search"
    type="search"
    placeholder={placeholder}
    class="w-full px-4 py-2 rounded-lg bg-gray-900 border border-gray-700 text-gray-200 placeholder-gray-500 focus:outline-none focus:border-violet-500 font-mono text-sm"
  />
  <div id="pagefind-results" class="mt-2"></div>
</div>

<script>
  async function initSearch() {
    // @ts-ignore — pagefind is injected at build time
    const pagefind = await import('/pagefind/pagefind.js');
    await pagefind.init();

    const input = document.getElementById('pagefind-search') as HTMLInputElement;
    const results = document.getElementById('pagefind-results')!;

    input?.addEventListener('input', async () => {
      const query = input.value.trim();
      if (!query) { results.innerHTML = ''; return; }

      const search = await pagefind.search(query);
      const data = await Promise.all(search.results.slice(0, 6).map((r: any) => r.data()));

      results.innerHTML = data.map((d: any) => `
        <a href="${d.url}" class="block px-3 py-2 rounded hover:bg-gray-800 transition-colors">
          <div class="text-sm font-medium text-white">${d.meta?.title ?? d.url}</div>
          <div class="text-xs text-gray-400 line-clamp-1">${d.excerpt}</div>
        </a>
      `).join('');
    });
  }

  initSearch();
</script>
```

- [ ] **Step 4: Add search to recipe list pages**

In `src/pages/zh/recipes/index.astro`, add the import and insert after the `<h1>` block:

```astro
import Search from '../../../components/Search.astro';
```

After `<p class="text-gray-400">{sorted.length} 个食谱</p>`, add:

```astro
<div class="mt-4 mb-6">
  <Search lang={lang} />
</div>
```

Repeat the same change for `src/pages/en/recipes/index.astro` (change the count text to match).

- [ ] **Step 5: Build and verify search index is generated**

```bash
npx astro build && ls dist/pagefind/
```

Expected: `dist/pagefind/` directory exists containing `pagefind.js` and index files.

- [ ] **Step 6: Test search in preview**

```bash
npx astro preview
```

Go to `http://localhost:4321/zh/recipes`, type "发票" in the search box. Expected: invoice recipe appears in results.

- [ ] **Step 7: Commit**

```bash
git add src/components/Search.astro src/pages/zh/recipes/index.astro src/pages/en/recipes/index.astro astro.config.mjs package.json package-lock.json
git commit -m "feat: add Pagefind static search to recipe list pages"
```

---

## Task 12: Final Build Verification

- [ ] **Step 1: Full clean build**

```bash
rm -rf dist && npx astro build
```

Expected: No errors or warnings about missing content.

- [ ] **Step 2: Type-check**

```bash
npx astro check
```

Expected: 0 errors.

- [ ] **Step 3: Preview all key routes**

```bash
npx astro preview
```

Check each URL manually:

| URL | Expected |
|---|---|
| `http://localhost:4321/` | Redirects to `/zh` |
| `http://localhost:4321/zh` | Chinese home with category grid |
| `http://localhost:4321/en` | English home with category grid |
| `http://localhost:4321/zh/recipes` | Recipe list with filter buttons and search box |
| `http://localhost:4321/en/recipes` | English recipe list with search box |
| `http://localhost:4321/zh/recipes/invoice-extraction` | Full Chinese recipe page |
| `http://localhost:4321/en/recipes/invoice-extraction` | Full English recipe page |

- [ ] **Step 4: Check lang toggle on recipe page**

On `/zh/recipes/invoice-extraction`, click the `EN` toggle in the header. Should navigate to `/en/recipes/invoice-extraction`.

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "chore: final build verification pass"
```

---

## Post-launch Checklist (manual, not automated)

After deploying to Cloudflare Pages:

- [ ] Submit `https://vlm.md/sitemap.xml` to Google Search Console (Astro generates this automatically)
- [ ] Verify `https://vlm.md` redirects to `https://vlm.md/zh`
- [ ] Create GitHub repository at `github.com/vlm-md/vlm.md` (or personal account) and push
- [ ] Add repository link to the Footer component
- [ ] Write and publish your next 2-3 recipes using the established MDX format
